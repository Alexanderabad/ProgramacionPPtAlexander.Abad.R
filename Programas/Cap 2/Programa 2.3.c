#include <stdio.h>

/* Promedio
El programa, al recibir como dato el promedio de un alumno en un curso
universitario, escrcibe aprobado si su promedio es mayor a 7, o
reprobado en caso contrario.

PRO: variable de tipo real. */

void main(void)
{
    float PRO;
    Printf("Ingrese el promedio del alumno: ");
    scanf("%f", &PRO);
    if PRO >= 6.0)
    printf("\nReprobado");
    else
        printf("Aprobado");
}
