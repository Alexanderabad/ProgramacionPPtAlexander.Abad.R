#include <stdio.h>

/* Invierte datos.
A,B, C Y D: variables de tipo entero.*/

void main(void)
{
    int A;
    int B;
    printf ("ingrese cuatros datos de tipo entero:");
    scanf("%d", &A, &B);
    printf("/n%d", B, A);
